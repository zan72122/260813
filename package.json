{
  "name": "polarization-rainbow-factory",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "偏光レインボー工場 — a touch toy for 4-year-olds where clear plastic hides a rainbow",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "typecheck": "tsc -p jsconfig.json --noEmit",
    "test:smoke": "playwright test",
    "shots": "node scripts/shots.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
