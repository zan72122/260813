{
  "name": "animal-choco-biscuit-factory",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Mobile web toy factory game: animal-printed hollow chocolate biscuits, for 4-year-olds.",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "lint": "eslint src tests",
    "test:e2e": "playwright test",
    "test:smoke": "playwright test tests/smoke.spec.js"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "eslint": "^9.39.0",
    "vite": "^8.2.1"
  }
}
