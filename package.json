{
  "name": "underwater-sand-castle",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview --host",
    "playtest": "node tools/fullrun.mjs",
    "playtest:robust": "node tools/robust.mjs"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "playwright": "^1.56.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  },
  "description": "水中砂城 — an underwater sand-castle building game for 4-year-olds (Three.js / WebGL2)"
}
